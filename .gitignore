# ─── Dependencias ───────────────────────────────────────────────────────────
node_modules/

# ─── Next.js build ──────────────────────────────────────────────────────────
.next/
out/

# ─── Variables de entorno (secretos) ────────────────────────────────────────
# CRÍTICO: nunca subir — contienen ADMIN_PASSWORD_HASH y ADMIN_SESSION_SECRET
.env
.env.local
.env.*.local
.env.production
.env.staging

# ─── Datos de usuario (privados, no pertenecen al repo) ─────────────────────
# (Temporalmente permitimos data/ para migrar presupuestos a Redis, luego lo bloquearemos)
# data/presupuestos.json
# data/

# ─── Imágenes subidas por el admin (binarios grandes, no código fuente) ──────
# Las imágenes actuales de reformas y tienda las subimos a git para que Vercel las sirva.
# Las nuevas imágenes que subas irán a Vercel Blob.

# ─── TypeScript ─────────────────────────────────────────────────────────────
next-env.d.ts
*.tsbuildinfo

# ─── SO y editores ──────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/settings.json

# ─── Logs ───────────────────────────────────────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ─── Herramientas internas (no código del proyecto) ─────────────────────────
.superpowers/
.claude/
.vercel
.env*
